/*
Exception -> These are error which arises in run time of a compiled program.
          -> It gives control of the program to another.
          -> It is built upon three keyword:
                1) try - it is put where an error is expected.
                       - When a error is incured it is thrown from try block
                       - A single try can have multiple catch blocks.
                2) catch - It catches the exception thrown from the try block.
                         - The code to handle the exception error is handled here.
                3) throw - A program throws an exeption using throw keyword.

*/
